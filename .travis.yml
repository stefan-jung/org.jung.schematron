language:
  - java 

jdk:
  - oraclejdk8

env:
  - DITA-OT-VERSION=3.1.2

before_script:
  - URL="https://github.com/dita-ot/dita-ot/releases/download/$DITA-OT-VERSION/dita-ot-$DITA-OT-VERSION.zip"
  - zip -qr org.doctales.schematron.zip . -x *.zip* *.git/* *temp/* *out/*
  - curl -LO $URL
  - unzip -q dita-ot-$DITA-OT-VERSION.zip
  - chmod +x dita-ot-$DITA-OT-VERSION/bin/dita
  - chmod +x dita-ot-$DITA-OT-VERSION/bin/ant
  - dita-ot-$DITA-OT-VERSION/bin/dita --install https://github.com/doctales/org.doctales.ant-contrib/archive/master.zip
  - dita-ot-$DITA-OT-VERSION/bin/dita --install org.doctales.schematron.zip
  - BASEDIR=$(dirname "$0")

script:
  - dita-ot-$DITA-OT-VERSION/bin/dita -i samples/sample.ditamap -f pdf2 -Dschematron.topic.validation.files=../rules/topic-validation.sch,../rules/topic-terminology.sch -DfailOnError.fatal=true -DfailOnError.error=true

notifications:
  slack:
    rooms:
      - doctales:BkI4k7awSFS5X5aT04Y3vHQ9#schematron
